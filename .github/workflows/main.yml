name: ci

on:
  push:
    branches:
      - "16.0"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3
      -
        name: Set image version
        run: |
          git_hash=$(git rev-parse --short HEAD)
      -
        name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - 
        name: Checkout to gitOps repo
        uses: actions/checkout@v3
        with:
          repository: Bigguy98/odoo-k8s
          token: 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
      - 
        name: Update version
        run: |
          current_version=$(cat odoo.yaml | grep image: | awk '{print $2}' | cut -d':' -f2)
          sed -i "s/$current_version/${GITHUB_REF_NAME##}-$git_hash/" odoo.yaml
          git config --global user.email "vuthanhtunghayquen98@example.com"
          git config --global user.name "Bugs Maker"
          git add .
          git commit -m "update odoo app version to "$git_hash
          git push
          
        
